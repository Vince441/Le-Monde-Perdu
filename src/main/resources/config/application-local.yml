spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://aws-1-eu-west-3.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.rtzjskwflcedtsqzcgkf
    password: ${PASSWORD}   # récupéré depuis secret.yml
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10   # < 15 pour laisser des connexions à DBeaver ou autres outils
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect